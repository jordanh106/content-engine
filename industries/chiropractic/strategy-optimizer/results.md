# Strategy Optimizer Results Log

Each entry records what changed, why, and what was predicted. Over time, this log reveals whether the changes are working.

**Metric:** weighted_engagement = (0.4 × save_rate) + (0.3 × share_rate) + (0.3 × comment_rate)

---

*No runs yet. Run "strategy optimizer" to generate the first entry.*
